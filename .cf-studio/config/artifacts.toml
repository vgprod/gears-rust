[[ignore]]
reason = "Ignore graph-storage research companions for ADR-0001 (graph-engine evaluation, PG19 SQL/PGQ spike report) — evidence documents, not Constructor Studio SDLC artifacts."
patterns = [
  "gears/graph-storage/docs/graph-engine-alternatives.md",
  "gears/graph-storage/docs/SPIKE-pg19-sqlpgq.md",
]

[[ignore]]
reason = "Ignore rust-traits.md — reference document for planned Rust SDK contracts, not a Constructor Studio SDLC artifact."
patterns = ["gears/system/resource-group/docs/rust-traits.md"]

[[ignore]]
reason = "Ignore 'gears/system' parent directory — not a module, only a grouping for system submodules."
patterns = ["gears/system"]

[[ignore]]
reason = "Ignore 'gears/bss' parent directory — not a module, only a grouping for BSS submodules."
patterns = ["gears/bss"]

[[ignore]]
reason = "Ignore 'gears/bss/libs' — not a module, only a grouping for shared BSS utility libraries."
patterns = ["gears/bss/libs"]

[[ignore]]
reason = "Ignore 'coord' shared utility library (distributed-lease primitive; infrastructure crate, not an SDLC-documented product module — no PRD/DESIGN required, README-documented)."
patterns = ["gears/bss/libs/coord/*"]

[[ignore]]
reason = "Ignore 'fixtures' joint golden conformance corpus (cf-gears-bss-fixtures + its conformance harness; test-support infrastructure crates consumed by the BSS gears, not an SDLC-documented product module — no PRD/DESIGN required, README-documented). Same class as 'coord' above."
patterns = ["gears/bss/fixtures/*"]

[[ignore]]
reason = "Ignore authn-resolver root module node while allowing nested plugin docs scopes."
patterns = ["gears/system/authn-resolver"]

[[ignore]]
reason = "Ignore 'gears/system/authz-resolver' (not SDLC-documented yet: missing required PRD/DESIGN)."
patterns = ["gears/system/authz-resolver/*"]

[[ignore]]
reason = "Ignore module 'cluster' traceability pending a full bidirectional traceability audit now that standalone-plugin and wiring/examples code has landed."
patterns = ["gears/system/cluster/*"]

[[ignore]]
reason = "Ignore module 'api-gateway' (not SDLC-documented yet: missing required PRD/DESIGN)."
patterns = ["gears/system/api-gateway/*"]

[[ignore]]
reason = "Ignore module 'grpc-hub' (not SDLC-documented yet: missing required PRD/DESIGN)."
patterns = ["gears/system/grpc-hub/*"]

[[ignore]]
reason = "Ignore module 'gear-orchestrator' (not SDLC-documented yet: missing required PRD/DESIGN)."
patterns = ["gears/system/gear-orchestrator/*"]

[[ignore]]
reason = "Ignore module 'nodes-registry' (not SDLC-documented yet: missing required PRD/DESIGN)."
patterns = ["gears/system/nodes-registry/*"]

[[ignore]]
reason = "Ignore oagw codebase and features — code traceability markers reverted, pending re-add. PRD/DESIGN/ADR/DECOMPOSITION still validated via autodetect."
patterns = [
  "gears/system/oagw/oagw/src/*",
  "gears/system/oagw/oagw/tests/*",
  "gears/system/oagw/oagw-sdk/src/*",
  "gears/system/oagw/docs/features/*",
]

[[ignore]]
reason = "Ignore legacy (pre-CFS) ADR files in oagw/docs root — converted copies live under docs/ADR/."
patterns = ["gears/system/oagw/docs/adr-*.md"]


[[ignore]]
reason = "Ignore usage-collector DESIGN, DECOMPOSITION, features and ADRs — they still describe the superseded model (counter/gauge usage kinds, event deactivation, signed compensation) and reference concepts the PRD no longer defines, pending the rewrite to aggregation folds and invalidation tracked in PRD section 13. PRD.md still validated via autodetect."
patterns = [
  "gears/system/usage-collector/docs/DESIGN.md",
  "gears/system/usage-collector/docs/DECOMPOSITION.md",
  "gears/system/usage-collector/docs/features/*",
  "gears/system/usage-collector/docs/ADR/*",
  "gears/system/usage-collector/usage-collector/src/*",
  "gears/system/usage-collector/usage-collector/tests/*",
  "gears/system/usage-collector/usage-collector-sdk/src/*",
]

[[ignore]]
reason = "Ignore the TimescaleDB usage-collector storage plugin — its specs and code still describe the superseded model and reference usage-collector concepts the PRD no longer defines (fr-usage-compensation, fr-event-deactivation, fr-usage-type-registration, fr-usage-type-deletion, fr-usage-type-existence-and-semantics), pending the plugin's own update to aggregation folds and invalidation once the rewrite tracked in PRD section 13 lands. Docs and code are ignored together: ignoring the specs alone would orphan the plugin's code traceability markers."
patterns = [
  "gears/system/usage-collector/plugins/timescaledb-usage-collector-plugin/docs/*",
  "gears/system/usage-collector/plugins/timescaledb-usage-collector-plugin/src/*",
  "gears/system/usage-collector/plugins/timescaledb-usage-collector-plugin/tests/*",
]

[[ignore]]
reason = "Ignore module 'tenant-resolver' (not SDLC-documented yet: missing required PRD/DESIGN)."
patterns = ["gears/system/tenant-resolver/*"]

[[ignore]]
reason = "Ignore module 'types' (not SDLC-documented yet: missing required PRD/DESIGN)."
patterns = ["gears/system/types/*"]

[[ignore]]
reason = "Ignore module 'types-registry' (not SDLC-documented yet: missing required PRD/DESIGN)."
patterns = ["gears/system/types-registry/*"]

[[ignore]]
reason = "Ignore module 'types-sdk' (not SDLC-documented yet: missing required PRD/DESIGN)."
patterns = ["gears/system/types-sdk/*"]

[[ignore]]
reason = "Ignore module 'simple-user-settings' (not SDLC-documented yet: missing required PRD/DESIGN)."
patterns = ["gears/simple-user-settings/*"]

[[ignore]]
reason = "Ignore module 'types-registry' (not SDLC-documented yet: missing required PRD/DESIGN)."
patterns = ["gears/types-registry/*"]

[[ignore]]
reason = "Ignore serverless-runtime companion docs — supplementary to DESIGN.md, not standalone SDLC artifacts."
patterns = [
  "gears/serverless-runtime/docs/DESIGN_GTS_SCHEMAS.md",
  "gears/serverless-runtime/docs/DESIGN_RUST_TYPES.md",
  "gears/serverless-runtime/docs/NEXT_ADR_SCOPE.md",
]

[[ignore]]
reason = "Ignore file-storage companion docs — api.md is a reference summary supplementary to DESIGN.md, and operations.md is an operator-facing config/runbook reference (P2 remediation item 3.8), not standalone SDLC artifacts."
patterns = [
  "gears/file-storage/docs/api.md",
  "gears/file-storage/docs/operations.md",
]

[[ignore]]
reason = "Ignore chat-engine companion doc — WEBHOOK-PROTOCOL.md documents the webhook JSON Schema (docs/webhook-protocol.json), the HTTP contract Chat Engine calls backend plugins over. A protocol reference supplementary to DESIGN.md, not a standalone SDLC artifact. Same class as the file-storage api.md/operations.md entry above."
patterns = ["gears/chat-engine/docs/WEBHOOK-PROTOCOL.md"]

[[ignore]]
reason = "Ignore module 'mini-chat' docs (temporary: PRD/DESIGN constraints failing in CI)."
patterns = ["gears/mini-chat/*"]

[[ignore]]
reason = "Ignore module 'approval-service' (PRD/DESIGN are placeholders). Remove this ignore after PRD/DESIGN conform to template."
patterns = ["gears/approval-service/*"]

[[ignore]]
reason = "Ignore module 'contracts' (PRD is a placeholder: sections 7-12 are TBD, including the Non-Functional Requirements the template requires at least one id under, and no DESIGN exists yet). Remove this ignore after PRD/DESIGN conform to template."
patterns = ["gears/bss/contracts/*"]

[[ignore]]
reason = "BSS multi-slice design sets (gears/bss/*/docs/design/*.md — pricing 12, rating 16, subscriptions 9 slices): DELIBERATELY outside cfs autodetect/traceability. cfs hardcodes the artifact-kind set, so a DESIGN_SLICE kind cannot be registered via the kit manifest (attempted + reverted 2026-07-28); a brace-glob on the DESIGN pattern breaks DESIGN.md matching. The slice docs follow the DESIGN template, declare cpt-cf-bss-* ids, and are review-audited (2026-07-22/28 waves) instead of cfs-validated. Revisit when cfs supports custom kinds — tracked in pricing DECISIONS.md (2026-07-28 review, finding 15)."
patterns = ["gears/bss/*/docs/design/*"]

[[systems]]
name = "cf-gears"
slug = "cf"
kit = "gears"

[[systems.autodetect]]
kit = "gears"
system_root = "{project_root}/gears/$system"
artifacts_root = "{system_root}/docs"

[systems.autodetect.artifacts]
[systems.autodetect.artifacts.PRD]
pattern = "PRD.md"
traceability = "FULL"
required = true

# Glob (not a literal name) so a gear may ship companion design documents
# alongside DESIGN.md — DESIGN-<aspect>.md, for an aspect large enough to
# carve out of the main design and own its own artifact. Each match is
# validated as a full DESIGN artifact against the kit constraints.
# Files that are NOT standalone SDLC artifacts must be listed in [[ignore]]
# above; ignore takes precedence over this pattern.
[systems.autodetect.artifacts.DESIGN]
pattern = "DESIGN*.md"
traceability = "FULL"
required = false

[systems.autodetect.artifacts.ADR]
pattern = "ADR/*.md"
traceability = "FULL"
required = false

[systems.autodetect.artifacts.DECOMPOSITION]
pattern = "DECOMPOSITION.md"
traceability = "FULL"
required = false

[systems.autodetect.artifacts.FEATURE]
pattern = "features/*.md"
traceability = "FULL"
required = false

[systems.autodetect.artifacts.UPSTREAM_REQS]
pattern = "UPSTREAM_REQS.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.validation]
require_kind_registered_in_kit = true
require_md_extension = true
fail_on_unmatched_markdown = true

[[systems.autodetect.codebase]]
name = "Gears"
path = "{system_root}/src"
extensions = [".rs"]

[[systems.autodetect.codebase]]
name = "Tests"
path = "{system_root}/tests"
extensions = [".rs"]

# Some gears/$system modules (e.g. file-storage) nest the crate a level deeper
# (gears/file-storage/file-storage/src, plus a sibling *-sdk crate) instead of
# putting the crate directly at gears/$system/src — mirrors the equivalent
# "Nested Gears"/"Nested Tests"/"Nested SDK"/"Nested SDK Tests" entries already
# used below for gears/system/$system. Without these, code under a nested
# crate layout is never scanned for @cpt traceability markers, so any to_code
# CDSL instruction there can never be satisfied regardless of the markers
# actually present in the code.
[[systems.autodetect.codebase]]
name = "Nested Gears"
path = "{system_root}/{system}/src"
extensions = [".rs"]

[[systems.autodetect.codebase]]
name = "Nested Tests"
path = "{system_root}/{system}/tests"
extensions = [".rs"]

[[systems.autodetect.codebase]]
name = "Nested SDK"
path = "{system_root}/{system}-sdk/src"
extensions = [".rs"]

[[systems.autodetect.codebase]]
name = "Nested SDK Tests"
path = "{system_root}/{system}-sdk/tests"
extensions = [".rs"]

[[systems.autodetect]]
kit = "gears"
system_root = "{project_root}/gears/system/$system"
artifacts_root = "{system_root}/docs"

[systems.autodetect.artifacts]
[systems.autodetect.artifacts.PRD]
pattern = "PRD.md"
traceability = "FULL"
required = true

[systems.autodetect.artifacts.DESIGN]
pattern = "DESIGN.md"
traceability = "FULL"
required = false

[systems.autodetect.artifacts.ADR]
pattern = "ADR/*.md"
traceability = "FULL"
required = false

[systems.autodetect.artifacts.DECOMPOSITION]
pattern = "DECOMPOSITION.md"
traceability = "FULL"
required = false

[systems.autodetect.artifacts.FEATURE]
pattern = "features/*.md"
traceability = "FULL"
required = false

[systems.autodetect.artifacts.UPSTREAM_REQS]
pattern = "UPSTREAM_REQS.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.validation]
require_kind_registered_in_kit = true
require_md_extension = true
# fail_on_unmatched_markdown is disabled at this scope because children autodetect rules
# (e.g. account-management/docs/tr-plugin) do not carve their paths out of the parent's
# unmatched-markdown walk, so tightening this would flag legitimate sub-plugin artifacts
# as unmatched.
#
# Known trade-off: because this autodetect block matches every gears/system/$system
# (oagw, resource-group, account-management, and any future sibling), setting this flag
# to `false` here also disables unmatched-markdown detection across every other
# gears/system/* subtree — not only account-management. Strictness is retained on each
# child scope (see the tr-plugin child below, which keeps fail_on_unmatched_markdown = true).
#
# Do NOT tighten this flag without first introducing a per-path carve-out mechanism for
# the tr-plugin-style sub-plugin case; otherwise the legitimate sub-plugin artifacts will
# re-surface as unmatched-markdown failures for every module that ships nested artifacts.
fail_on_unmatched_markdown = false

[[systems.autodetect.codebase]]
name = "Gears"
path = "{system_root}/src"
extensions = [".rs"]

[[systems.autodetect.codebase]]
name = "Tests"
path = "{system_root}/tests"
extensions = [".rs"]

[[systems.autodetect.codebase]]
name = "Nested Gears"
path = "{system_root}/{system}/src"
extensions = [".rs"]

[[systems.autodetect.codebase]]
name = "Nested Tests"
path = "{system_root}/{system}/tests"
extensions = [".rs"]

[[systems.autodetect.codebase]]
name = "Nested SDK"
path = "{system_root}/{system}-sdk/src"
extensions = [".rs"]

[[systems.autodetect.codebase]]
name = "Nested SDK Tests"
path = "{system_root}/{system}-sdk/tests"
extensions = [".rs"]

[[systems.autodetect.codebase]]
name = "Nested SDK Examples"
path = "{system_root}/{system}-sdk/examples"
extensions = [".rs"]

# Plugin crates that carry the gear's own name and a role suffix
# (gears/system/$system/plugins/$system-<role>-plugin) realize the gear's plugin
# contracts and carry @cpt markers for the parent gear's feature IDs. They ship no
# docs of their own, so their code belongs to the parent scope. Codebase paths are
# literal (no globs), one entry per role. Plugins named after a vendor or backend
# (static-idp-plugin, keycloak-idp-plugin, timescaledb-usage-collector-plugin) are
# not affected; the ones with their own docs keep their child scopes below.
[[systems.autodetect.codebase]]
name = "Storage Plugin"
path = "{system_root}/plugins/{system}-storage-plugin/src"
extensions = [".rs"]

[[systems.autodetect.codebase]]
name = "Storage Plugin Tests"
path = "{system_root}/plugins/{system}-storage-plugin/tests"
extensions = [".rs"]

# Nested sub-plugin scope: account-management/docs/tr-plugin/ carries its own PRD/DESIGN/ADR.
# Declared as a child of the `gears/system/$system` autodetect rule so the sub-plugin
# artifacts are detected as belonging to tr-plugin, not to account-management.
[[systems.autodetect.children]]
kit = "gears"
system_root = "{project_root}/gears/system/account-management/docs/tr-plugin"
artifacts_root = "{system_root}"

[systems.autodetect.children.artifacts]
[systems.autodetect.children.artifacts.PRD]
pattern = "PRD.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.children.artifacts.DESIGN]
pattern = "DESIGN.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.children.artifacts.ADR]
pattern = "ADR/*.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.children.artifacts.DECOMPOSITION]
pattern = "DECOMPOSITION.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.children.artifacts.FEATURE]
pattern = "features/*.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.children.validation]
require_kind_registered_in_kit = true
require_md_extension = true
fail_on_unmatched_markdown = true

# Nested sub-plugin scope: Keycloak IdP plugin carries its own PRD/DESIGN/DECOMPOSITION
# under gears/system/account-management/plugins/keycloak-idp-plugin/docs.
[[systems.autodetect.children]]
kit = "gears"
system_root = "{project_root}/gears/system/account-management/plugins/keycloak-idp-plugin/docs"
artifacts_root = "{system_root}"

[systems.autodetect.children.artifacts.PRD]
pattern = "PRD.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.children.artifacts.DESIGN]
pattern = "DESIGN.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.children.artifacts.ADR]
pattern = "ADR/*.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.children.artifacts.DECOMPOSITION]
pattern = "DECOMPOSITION.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.children.artifacts.FEATURE]
pattern = "features/*.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.children.validation]
require_kind_registered_in_kit = true
require_md_extension = true
fail_on_unmatched_markdown = true

# Nested sub-plugin scope: OIDC AuthN plugin carries its own PRD/DESIGN/DECOMPOSITION
# under gears/system/authn-resolver/plugins/oidc-authn-plugin/docs.
[[systems.autodetect.children]]
kit = "gears"
system_root = "{project_root}/gears/system/authn-resolver/plugins/oidc-authn-plugin/docs"
artifacts_root = "{system_root}"

[systems.autodetect.children.artifacts]
[systems.autodetect.children.artifacts.PRD]
pattern = "PRD.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.children.artifacts.DESIGN]
pattern = "DESIGN.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.children.artifacts.ADR]
pattern = "ADR/*.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.children.artifacts.DECOMPOSITION]
pattern = "DECOMPOSITION.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.children.artifacts.FEATURE]
pattern = "features/*.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.children.validation]
require_kind_registered_in_kit = true
require_md_extension = true
fail_on_unmatched_markdown = true

# Nested sub-plugin scope: TimescaleDB usage-collector storage plugin carries its own
# PRD/DESIGN under gears/system/usage-collector/plugins/timescaledb-usage-collector-plugin/docs.
[[systems.autodetect.children]]
kit = "gears"
system_root = "{project_root}/gears/system/usage-collector/plugins/timescaledb-usage-collector-plugin/docs"
artifacts_root = "{system_root}"

[systems.autodetect.children.artifacts]
[systems.autodetect.children.artifacts.PRD]
pattern = "PRD.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.children.artifacts.DESIGN]
pattern = "DESIGN.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.children.artifacts.ADR]
pattern = "ADR/*.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.children.artifacts.DECOMPOSITION]
pattern = "DECOMPOSITION.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.children.artifacts.FEATURE]
pattern = "features/*.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.children.validation]
require_kind_registered_in_kit = true
require_md_extension = true
fail_on_unmatched_markdown = true


[[systems.autodetect]]
kit = "gears"
slug = "cf-bss"
system_root = "{project_root}/gears/bss/$system"
artifacts_root = "{system_root}/docs"

[systems.autodetect.artifacts]
[systems.autodetect.artifacts.PRD]
pattern = "PRD.md"
traceability = "FULL"
required = true

[systems.autodetect.artifacts.DESIGN]
pattern = "DESIGN.md"
traceability = "FULL"
required = false

[systems.autodetect.artifacts.ADR]
pattern = "ADR/*.md"
traceability = "FULL"
required = false

[systems.autodetect.artifacts.DECOMPOSITION]
pattern = "DECOMPOSITION.md"
traceability = "FULL"
required = false

[systems.autodetect.artifacts.FEATURE]
pattern = "features/*.md"
traceability = "FULL"
required = false

[systems.autodetect.artifacts.UPSTREAM_REQS]
pattern = "UPSTREAM_REQS.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.validation]
require_kind_registered_in_kit = true
require_md_extension = true
fail_on_unmatched_markdown = false

[[systems.autodetect.codebase]]
name = "Gears"
path = "{system_root}/src"
extensions = [".rs"]

[[systems.autodetect.codebase]]
name = "Tests"
path = "{system_root}/tests"
extensions = [".rs"]

[[systems.autodetect.codebase]]
name = "Nested Gears"
path = "{system_root}/{system}/src"
extensions = [".rs"]

[[systems.autodetect.codebase]]
name = "Nested Tests"
path = "{system_root}/{system}/tests"
extensions = [".rs"]

[[systems.autodetect.codebase]]
name = "Nested SDK"
path = "{system_root}/{system}-sdk/src"
extensions = [".rs"]

[[systems]]
name = "examples"
slug = "examples"
kit = "gears"

[[systems.autodetect]]
kit = "gears"
system_root = "{project_root}/docs/spec-templates/examples/$system"
artifacts_root = "{system_root}"
codebase = []

[systems.autodetect.artifacts]
[systems.autodetect.artifacts.PRD]
pattern = "PRD.md"
traceability = "DOCS-ONLY"
required = true

[systems.autodetect.artifacts.DESIGN]
pattern = "DESIGN.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.artifacts.ADR]
pattern = "ADR/*.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.artifacts.DECOMPOSITION]
pattern = "DECOMPOSITION.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.artifacts.FEATURE]
pattern = "features/*.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.artifacts.UPSTREAM_REQS]
pattern = "UPSTREAM_REQS.md"
traceability = "DOCS-ONLY"
required = false

[systems.autodetect.validation]
require_kind_registered_in_kit = true
require_md_extension = true
fail_on_unmatched_markdown = true
